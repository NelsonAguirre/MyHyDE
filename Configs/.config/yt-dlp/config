# Selección de formato (Prioriza AVC1/H.264 para compatibilidad)
-f "bestvideo[vcodec^=avc1][height<=1080]+bestaudio/best[vcodec^=avc1][height<=1080]/bestvideo[vcodec^=avc1][height<=720]+bestaudio/best[vcodec^=avc1][height<=720]/best"

# Post-procesamiento y Metadatos
--merge-output-format mkv
--embed-metadata
--embed-chapters
--embed-thumbnail
--embed-subs
--sub-langs "en.*"

# SponsorBlock (Marca las secciones de patrocinadores en la línea de tiempo)
--sponsorblock-mark all
